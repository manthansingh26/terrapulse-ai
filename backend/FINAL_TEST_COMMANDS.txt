========================================
TERRAPULSE AI - EMAIL ALERTS FINAL TEST
========================================

STEP 1: CONFIGURE GMAIL CREDENTIALS
====================================

Edit backend/.env and update these lines:
  SMTP_USER=your-mansa26012008@gmail.com
  SMTP_PASSWORD=1234567812345678
  ALERT_EMAIL=your-mansa26012008@gmail.com

Get App Password from: https://myaccount.google.com/apppasswords
(Select Mail + Windows Computer)

STEP 2: START BACKEND
=====================

Open PowerShell and run:

cd d:\Projects\terrapulse-ai\backend
uvicorn app.main:app --reload

Wait for: "Uvicorn running on http://0.0.0.0:8000"

STEP 3: TEST EMAIL ALERTS (COPY & PASTE)
==========================================

Open NEW PowerShell window and paste this:

$testBody = @{
    city = "Ahmedabad"
    aqi_value = 250
} | ConvertTo-Json

$response = Invoke-RestMethod -Uri "http://localhost:8000/api/alerts/test" `
  -Method Post `
  -ContentType "application/json" `
  -Body $testBody

Write-Host "Test Result:"
$response | ConvertTo-Json

STEP 4: CHECK YOUR EMAIL
========================

Go to your Gmail inbox and look for:
  Subject: "High AQI Alert - Ahmedabad (AQI: 250)"
  From: your-email@gmail.com

If you see this email → SUCCESS!

VERIFICATION CHECKLIST
======================

[✓] Backend starts without errors
[✓] No "Not authenticated" error
[✓] Test endpoint responds with success
[✓] Email received in inbox
[✓] Dashboard shows red alert banner
[✓] All systems working!

IF EMAIL DOESN'T SEND
=====================

1. Check backend console for errors
2. Verify SMTP credentials in .env
3. Make sure Gmail app password (16 digits) is correct
4. Check firewall isn't blocking SMTP port 587
5. Enable "Less secure app access" if needed

NEXT STEPS
==========

1. Start both backend and frontend
2. Go to http://localhost:3000
3. Dashboard will show red alert banner if any city AQI > 200
4. Alerts send automatically when saving high AQI data

========================================
THAT'S IT! EMAIL ALERTS ARE NOW READY!
========================================
